-- Vouchers.sq

CREATE TABLE Vouchers (
    GUID TEXT,
    DATE TEXT,
    NARRATION TEXT,
    PARTYLEDGERNAME TEXT,
    VOUCHERNUMBER TEXT,
    VCHTYPE TEXT,
    D1 REAL
);

selectAll:
SELECT * FROM Vouchers;

selectByGuid:
SELECT * FROM Vouchers WHERE GUID = ?;